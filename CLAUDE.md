# 合同会社らいとすぴりっつ コーポレートサイト

https://lightspirits.jp/ — 最終更新 2026-08-19

## このサイトについて

AI・ロボット・アプリ開発を「ひとつの窓口で」請ける会社のコーポレートサイト。
個人・クリエイターと法人の両方が読者。マスコット（雷神・もりけん）が案内役として常駐する。

**主役はHPの本文。** マスコットと会話は、読んでいる途中を少し楽しくする脇役。
この優先順位は繰り返し確認された方針なので、迷ったら本文を優先すること。

## 技術構成 — 最初に読むこと

- **フレームワークもビルドもない。** package.json も node_modules も存在しない
- `index.html` が全部入り（約5,000行）。CSSは `<style>`、JSは末尾の `<script>` にインライン
- 変更したら `<style>` の波括弧の対応を必ず確認する（後述の事故例）
- デプロイは **GitHub Pages（main ブランチのルート）**。push すれば数分で本番
- CI は `.github/workflows/check.yml` → `scripts/check.js`（依存なし、Nodeのみ、数秒）
  - **検査が赤くてもサイトの公開は止まらない。** 気づくための仕組み
  - ローカル実行: `node scripts/check.js`

## ファイル構成

| ファイル | 役割 |
|---|---|
| `index.html` | サイト本体。全画面幅をこれ1枚で担当 |
| `data/conversations.js` | 「もりけんと雷神の話」セクションの会話（145件・11カテゴリー） |
| `data/bubbles.js` | マスコットが喋る内容（単発30・一言かけあい52・雑談104） |
| `data/news.js` | NEWSティッカー（現在0件＝ティッカー非表示） |
| `data/README.md` | 上記3つの書き方。**会話を足す前に必ず読む** |
| `scripts/check.js` | CI検査の本体 |
| `privacy.html` / `terms.html` | ポリシー（独立ページ、CSSインライン） |
| `thanks.html` | フォーム送信後 |
| `mobile.html` | **廃止済み。** トップへ転送する18行だけ。中身を戻さないこと |
| `PROJECT_NOTES.md` / `draft*.html` / `lp-natural.html` ほか | 本作業と無関係な既存物。触らない |

## 絶対に変えないほうがよいこと

1. **`data-theme="general"` を `<html>` から外さない。**
   土台のCSSは企業向け（黒×金）で、一般向けは **393個の上書き**で成り立っている。
   属性を外すと全ページが黒くなる。2026-08-18にテーマ統合したが、この構造は残したまま
2. **モバイル専用レイアウトを作らない。** 2026-08-18に `mobile.html` を廃止し、
   index.html が全幅を担当する方針にした。viewport・ブレークポイントを作り直さない
3. **新しいブレークポイントを増やさない。** 既存は 900px（レイアウト）/ 620px（ヘッダー）/
   1100px・820px（歯車）/ 700px（会話の開閉）。原則 900px を使う
4. **夜空の配色はセクションIDに紐づいている。** セクションを並べ替えると配色が壊れる（後述）
5. **会話データの `id` を変更・再利用しない**
6. GitHub Actions・secrets・branch設定・Pages設定を変えない

## デザインのルール

### 配色（`:root` と `[data-theme="general"]`）

- 企業向け（土台）: 金 `#C9A84C` / 黒 `#050505` / 生成り `#F5F0E8`
- 一般向け（実際に表示される方）: 青 `#0284C7` / 白 `#FFFFFF` / 濃紺 `#0C3A56`
- 夜空側（ページ下半分）: 文字 `#EEF4FE` / 金 `#E8C97A` / ガラス `rgba(12,22,42,0.55)`

### フォント

Noto Sans JP（本文）/ Zen Maru Gothic（見出し・丸ゴシック）/ Space Grotesk（英字見出し）/
Space Mono（ラベル・日付）/ Shippori Mincho（月の和名）

### 夜空の背景（一般向けの要）

`<div class="night-sky">` を body 直下に `z-index:-1` で全高に敷く。名刺のデザイン由来。

- 上端はクリーム `#F8F4EA`（名刺おもて）→ 下端は夜空 `#03060E`（名刺裏）
- **明暗の変わり目は 50%**。`#yui`（濃い文字）と `#dialogue`（明るい文字）の境目に合わせてある
- 含まれるもの: 星野・星座8つ・黄金螺旋・遊星歯車（ヒーロー左）・月（満ち欠け）・
  向日葵配列（YUi中心）・遠雷（地平線）・地平線（フッター直前）
- **記事フィードでページ高さが変わるため、地平線と向日葵はJSで実測して配置**
  （`--ns-horizon-y` / `--ns-phyllo-y`）

### セクションの並び（変更は慎重に）

```
hero → news → about → works → services → strength → yui
→ dialogue → raizin → blog → founder → faq → company → contact → footer
```

**夜側の配色を持つセクションが下半分に固まる並び**を選んである。
並べ替えるときは、動かすセクションの文字色と、その位置の空の明るさを必ず測り直すこと。

## マスコットと会話 — 役割分担

| | 担当 | データ |
|---|---|---|
| **固定マスコット**（画面隅の立ち絵＋下中央の会話ウィンドウ） | 愛嬌担当。ふと目に入る | `data/bubbles.js` |
| **Dialogueセクション**（ページ中ほど） | 読ませる会話 | `data/conversations.js` |

この分担は何度か揺れた末に固まったもの。**両方が会話をやろうとすると両方が中途半端になる**、
というのがユーザーの判断。

### 会話ウィンドウ（2026-08-19に吹き出しから変更）

- 画面下中央に**1枚だけ**。720×104px、`z-index:195`（ナビ200より下）
- **1度に1せりふ**。名前＋本文が入れ替わる。しっぽや三角は付けない
- 自動進行。4〜6秒/行、会話後10〜20秒（長い会話ほど+2.5秒/行）休止
- ウィンドウか立ち絵をクリックで早送り。**クリックしないと進まない仕様にはしない**
- × で閉じるとそのタブでは**自動では出なくなる**（`sessionStorage: ls-talk-closed`）。
  ただし立ち絵を押せば1会話だけ出る。閉じてもクリック操作は死なせない
- Contact・Footerが画面下部に来ると黙る
- 立ち絵は出入りせず、**薄くもしない**（opacity 1）。喋る側だけ scale 1.04
- 調整用の値はスクリプト冒頭: `LINE_MIN` `LINE_MAX` `REST_MIN` `REST_MAX`

### 会話を追加するとき

**`data/` のファイルだけを編集する。** index.html・CSS・CIを触る必要はない。
書式は `data/README.md` を参照。CI検査が形式を守らせる。

## 会社情報（記入済み）

商号 合同会社らいとすぴりっつ（ライトスピリッツ）/ 英文 lightspirits /
代表社員 森田健太 / 所在地 茨城県守谷市けやき台３丁目８－１５ /
設立 2026年8月8日 / 法人番号 5050003010326 / info@lightspirits.jp /
管轄 水戸地方裁判所龍ケ崎支部 / ポリシー制定日 2026年8月8日

**推測で会社情報を書かないこと。** 未確定なら TODO コメントと `準備中` 表示で残す。

## 過去に起きた事故（同じ轍を踏まないこと）

1. **CSSセレクタ列の断裂** — ルールを削除したら、それが見出し用セレクタ列の
   *終端*だったため、列が直後の `#hero` に合流し全見出しに背景が付いた。
   **波括弧の数は合うので構文チェックでは検出できない。** 計算スタイルの実測で発見
2. **セクション並べ替えでマークアップが消えた** — セクション間にあった
   代表プロフィールのポップアップを、抽出スクリプトが収集しながら出力していなかった。
   並べ替えは**前後の要素も含めて**扱うこと
3. **書き換えたIIFEの変数を別の処理が参照していた** — ドラッグ処理が動かなくなった。
   スクリプトを差し替えたら、**コンソールエラーを必ず確認する**
4. **自身のソースから再生成して二重エスケープ** — `\n` が `\\n` になった。
   データを作り直すときは、パース済みの値から作る

## 検証について

- プレビュータブは `visibilityState: hidden` を返すことがあり、**タイマーが抑制される**。
  自動進行の「間隔」はこの環境では測れない（行間が56秒に伸びた実績あり）
- スクリーンショットが真っ白になることがある。**数値計測（getBoundingClientRect・
  getComputedStyle）を主軸にする**
- ふきだしの出入りは1往復のツール呼び出しより速い。
  一時的に MutationObserver を仕込んで観測し、**終わったら必ず外してdiffで確認する**
